pytest>=9
pdfplumber>=0.11
pyyaml>=6
scikit-learn>=1.9
numpy>=1.26
# GOOGLE_API_KEY가 있을 때만 실제로 import됨(app/retrieval.py의 GeminiEncoder) —
# 키 없이 개발 중인 지금은 설치 안 해도 TF-IDF 대체 경로로 전부 동작함
# langchain-google-genai>=2
