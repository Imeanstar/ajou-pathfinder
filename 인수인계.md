# 2차 프로젝트 인수인계서

> 이 문서는 새 대화창(새 세션)에서 이어받기 위한 문서입니다. 세션 시작 시 이 파일을 첨부하거나 내용을 붙여넣고 "이 인수인계서 기준으로 2차 프로젝트 시작하자"라고 말하면 됩니다.

---

## 0. 지금까지의 흐름 (한눈에)

아주대 AI 부트캠프 "실전 AI 개발 1" 과목 진행 중이며, 순서는 다음과 같았습니다.

1. **Part 1 학습** (Textbook_Part-1.pdf, 409p) — Gemini API, Agent Studio, Agent Platform 검색/그라운딩, 프롬프트 아키텍처, LangChain RAG, TravelBot 구현 등. 퀴즈 대비 자료까지 제작 완료.
2. **PBL 1차 MVP 프로젝트** — 경기도 공무원 업무추진비 공공데이터 + 카카오 API 기반 "예산 지출 투명성 Q&A 챗봇"을 4일간 개발. **이미 완료·제출됨.** 결과물은 `../프로젝트/` 및 `../Dev/` 폴더에 있음 (2조, 소프트웨어학과 202126876 백민우 제출본).
3. **Part 2 학습** (Part_2.pdf, 256p) — Google ADK(Agent Development Kit)를 중심으로 멀티 에이전트 시스템, MCP, A2A, CI/CD, 평가, 보안, FinOps까지 학습. 퀴즈 대비 치트시트 + 50문항 모의고사 아티팩트 제작 완료.
4. **지금**: Part 2까지 학습을 마쳤고, 이 내용을 바탕으로 **"2차 프로젝트"**를 시작해야 하는 단계.

## 1. 2차 프로젝트 관련 — 아직 확인되지 않은 것 (⚠️ 새 세션에서 최우선으로 확인)

**2차 프로젝트의 공식 요구사항 문서를 아직 받지 못한 상태입니다.** PBL 1차 때는 `PBL_1차_MVP_4일_프로젝트_작성_안내.pdf`와 통합 제출양식(.docx)이 먼저 주어졌지만, 2차는 아직 그런 문서가 없습니다. 새 세션 시작 시 사용자에게 아래를 확인해야 합니다.

- [ ] 2차 프로젝트 공식 안내 문서(PDF/DOCX)가 있는지, 있다면 파일 경로
- [ ] 개인 프로젝트인지 팀 프로젝트인지, 마감일(기간)
- [ ] 제출 양식이 정해져 있는지 (PBL 1차처럼 SECTION 구조인지)
- [ ] **ADK 사용이 필수인지** — Part 2 강의 내용상 십중팔구 ADK 기반 에이전트 프로젝트일 가능성이 높음
- [ ] 자유 주제인지, 특정 도메인/데이터가 지정되는지
- [ ] PBL 1차 결과물(예산 투명성 챗봇)을 확장하는 것인지, 완전히 새로운 주제인지

## 2. Part 2에서 배운 내용 (상세 — 2차 프로젝트의 재료)

Part 2는 **Google ADK(Agent Development Kit)**를 중심축으로, 에이전트를 실제로 설계·개발·배포·운영하는 전 과정을 다뤘습니다. 아래는 2차 프로젝트를 설계할 때 "무엇을 쓸 수 있는가"의 재료 목록입니다.

### 2-1. ADK 기초
- ADK는 **결정론적 코드 제어**와 **LLM의 유연한 추론** 사이에서 균형을 잡는 오픈소스 프레임워크(Python SDK). Gemini Enterprise(완성형, 쉬움)와 LangChain/직접빌드(저수준, 유연) 사이에 위치.
- 핵심 구성요소: 에이전트, 도구(Tools), 조정(Orchestration), 콜백, 세션 관리, 메모리, 평가, 배포, 코드 실행, 계획(Planning), 디버깅, Trace, 모델.

### 2-2. 에이전트 개발 기본
- 프로젝트 구조: `agent.py`에 **`root_agent`** 변수 필수, `__init__.py`에 import 포함.
- 설치: `pip install google-adk`, 가상환경 `python3 -m venv .adk`.
- `Agent(model=..., name=..., description=..., instruction=...)` — `description`은 멀티 에이전트에서 라우팅(어떤 에이전트를 호출할지 판단)에 사용되므로 중요.
- 상호작용 방법 4가지: `adk web`(로컬 UI), `adk run`(CLI), `adk api_server`(REST, **프로덕션 적합**), 프로그래매틱(Python SDK로 앱에 임베드, **프로덕션 적합**).
- 도구(Tools): 함수의 **docstring**으로 도구 선언 자동 생성. LLM이 `FunctionCall`을 반환하면 ADK가 실행 후 `FunctionResponse`로 결과 반환.
- **콜백 6종**: `before/after_agent_callback`(실행 스킵·정리), `before/after_model_callback`(가드레일·캐싱·로깅), `before/after_tool_callback`(권한검사·결과후처리).

### 2-3. 컨텍스트 관리 (세션 · 상태 · 메모리)
- LLM은 stateless → 대화 유지를 위한 3계층: **Session**(현재 대화), **State**(세션 내 임시 스크래치패드), **Memory**(세션 간 장기 기억).
- SessionService: `InMemorySessionService`(개발용, 재시작 시 손실) / `VertexAiSessionService`(Agent Runtime 필요, 관리형) / `DatabaseSessionService`(PostgreSQL 등).
- State: `output_key`로 자동 저장, `context.state[...]`로 직접 조작, instruction에 `{key}` 템플릿 삽입 가능.
- Memory: `InMemoryMemoryService`(단순 검색) / `VertexAiMemoryBankService`(임베딩 기반 유사성 검색). `load_memory` 기본 도구 제공.
- Example Store: 퓨샷 예시를 임베딩 기반으로 관리·검색, 여러 에이전트 간 공유 가능.

### 2-4. 멀티 에이전트 시스템
- **LLM 기반 에이전트**(비결정론적, 스스로 판단) vs **워크플로 에이전트**(LLM 미연결, 결정론적, "감독자" 역할).
- 워크플로 에이전트 4유형: `SequentialAgent`(순차), `LoopAgent`(종료조건까지 반복), `ParallelAgent`(동시실행), `Custom Agent`(조건부/복잡한 로직).
- 계층 구조: `root_agent` → `sub_agent`(피어 관계로 여러 개) → 각자 `tool` 보유.

### 2-5. 생태계 & 배포
- Google 에이전트형 스택: Gemini Enterprise / SaaS 플랫폼 통합 / 맞춤형 클라이언트, 그 아래 Gemini Enterprise Agent Platform(ADK + Agent Runtime + Agent Garden).
- **배포 결정 트리**: 완전관리형 원함 → **Agent Runtime** / 서버리스 인프라 원함 → **Cloud Run**(Scale-to-zero, 비용효율) / 컨테이너화 가능 → **GKE** / 아니면 Compute Engine.
- Agent Runtime이 대신 처리: 세션·메모리 DB, IAM/서비스계정, 평가, 모니터링/로깅/추적, 스트리밍 API, 멀티모달 등 — **에이전트 로직 자체는 직접 설계해야 함**.

### 2-6. MCP (Model Context Protocol)
- 문제: 에이전트×도구서버 다대다 통합 문제 → **도구 통합의 개방형 표준**.
- 3대 요소: 도구(Tool), 리소스(Resource), 프롬프트(Prompt). 에이전트=호스트, MCP 클라이언트가 서버에 연결.
- ADK 사용: `McpToolset(connection_params=StreamableHTTPConnectionParams(url=...))`. 서버 빌드: `FastMCP` + `@mcp.tool`.
- 인증: 서비스 계정(간편, 권한 동일) vs OAuth(사용자별, 복잡).

### 2-7. A2A (Agent2Agent)
- 문제: 프레임워크가 다른 에이전트(ADK/CrewAI/LangGraph) 간 통신 → **HTTP 기반 JSON-RPC** 개방형 프로토콜.
- 에이전트 카드(`/.well-known/agent-card.json`)가 에이전트의 "명함" 역할.
- ADK 활용: `RemoteA2aAgent`(원격 에이전트 사용) / `to_a2a()`(에이전트 노출) / `adk api_server --a2a`.
- A2A는 **팀 간 소유 분리·독립 확장이 필요할 때** 쓰고, 단순·단일팀·저지연 요구 시스템엔 오버엔지니어링.

### 2-8. CI/CD
- 배포 대상 3종: Agent Runtime / Cloud Run / GKE.
- GCP 도구: Artifact Registry(아티팩트 저장), Cloud Build(빌드 자동화, `cloudbuild.yaml`), Cloud Deploy(배포 파이프라인), Secret Manager(비밀 관리), Terraform(IaC).
- `cloudbuild.yaml` 커스텀 변수는 **밑줄(_)로 시작** 규칙.

### 2-9. 에이전트 평가 (ADK Eval)
- 핵심 원칙: **챗봇은 답변 기준, 에이전트는 결정·결과(궤적) 기준**으로 평가.
- 성숙도 모델: 출력테스트 → 도구/궤적검증 → 데이터셋 기반 회귀테스트 → CI/CD+프로덕션 모니터링.
- 테스트 파일(단위테스트, 빠름, 개발중) vs evalset 파일(통합테스트, 복잡한 다중턴). `adk web` UI로 evalset 생성 가능.
- 실행: `adk web` / `adk eval` / `pytest`(`AgentEvaluator.evaluate`, **CI 통합 가능**).

### 2-10. 보안
- LLM 4대 위험: 악성 URL, 프롬프트 인젝션/탈옃, 민감정보(PII) 노출, 불쾌감을 주는 자료.
- **Sensitive Data Protection(SDP)/DLP API**: infoType 기반 PII 탐지·마스킹·토큰화.
- **Model Armor**: 프롬프트/응답 검사 별도 서비스, 4대 안전 필터(괴롭힘/증오심표현/음란물/위험한콘텐츠) + SDP 연동.

### 2-11. FinOps (비용 관리)
- 비용 구조: 모델(토큰) + 실행(도구/API) + 인프라 + 모니터링.
- 절감 전략: 프롬프트 최적화(토큰 감소), 응답 캐싱(`ContextCacheConfig`, **애플리케이션 수준**), 모델 적정 크기(가장 작은 모델 우선), 동적 모델 라우팅.
- GCP 표준 관행: BigQuery 결제 내보내기, 라벨 기반 비용 할당, 세션당 비용 모니터링.

## 3. Part 2 실습 파일 (직접 실습 가능한 자료, ADK 코드 참고용)

경로: `../Part 2/`

| 파일명 | 다루는 내용 |
|---|---|
| 01. 에이전트 개발 키트(ADK) 시작하기.docx | ADK 설치, 기본 에이전트 생성, 프로젝트 구조 |
| 02. 도구로 ADK 에이전트 지원.docx | Tools(함수 도구) 작성·연결 |
| 03. ADK로 멀티 에이전트 시스템 빌드.docx | Sequential/Loop/Parallel/Custom 에이전트 |
| 04. ADK 에이전트를 Agent Engine에 배포.docx | Agent Runtime 배포 실습 |
| 02. 모델 컨텍스트 프로토콜(MCP) 도구를 ADK 에이전트와 함께 사용.docx | MCP 클라이언트/서버 실습 |
| 03. ADK 및 Agent2Agent(A2A) SDK로 원격 에이전트에 연결.docx | A2A 원격 에이전트 실습 |
| 01. Terraform 및 Cloud Build를 사용한 에이전트용 CI/CD 파이프라인 빌드.docx | Terraform + Cloud Build 실습 |
| 01. 세션 및 메모리 서비스를 사용하여 ADK 에이전트 빌드.docx | Session/State/Memory 실습 |
| 추가 종합 실습.docx | 종합 실습 (여러 개념 통합) |

`.zip` 실습 소스 코드도 같은 폴더에 있음: `adk_project.zip`, `adk_tools.zip`, `adk_multiagent_systems.zip`, `adk_mcp_tools.zip`, `adk_and_a2a.zip`, `adk_to_agent_engine.zip`, `cicd-lab-agent.zip` — **2차 프로젝트 스캐폴딩 시 이 코드들을 참고/재사용 가능**.

## 4. Part 1 학습 내용 (참고용, 개요만)

경로: `../실습/`. Gemini API·Agent Studio·Agent Platform 검색(그라운딩)·프롬프트 아키텍처·LangChain RAG·Agent Search 통합·TravelBot 구현·프롬프트 엔지니어링을 다룸. 2차 프로젝트가 RAG나 검색 그라운딩을 필요로 한다면 이 폴더의 실습 코드도 참고 대상.

## 5. 이미 만들어둔 학습 참고 자료 (Part 2 요약)

이번 세션에서 Part_2.pdf 256페이지 전체를 읽고 아래 두 개를 아티팩트로 제작·배포했습니다. 2차 프로젝트 설계 중 ADK 개념이 헷갈리면 참고.

- **ADK 치트시트** (11개 섹션 요약 + 핵심 코드 + 비교표): `https://claude.ai/code/artifact/44215f61-226d-4269-a7fb-65bcb54e069b`
- **ADK 모의고사 50제** (4지선다, 즉시채점): `https://claude.ai/code/artifact/56d84bac-5c1e-4557-aa2f-256881b8ef5e`

## 6. PBL 1차 프로젝트 결과물 (참고/재사용 가능)

- 위치: `../프로젝트/` (소스코드, FastAPI 앱, RAG 파이프라인 등), `../Dev/` (최종 제출 zip/pdf/시연영상)
- 주제: 경기도 업무추진비 공공데이터 + 카카오 API 기반 "예산 지출 투명성 Q&A 챗봇" ("공무원 또간집" 아이디어에서 피벗)
- 이미 RAG 파이프라인, 데이터 전처리(프랜차이즈 판별·지점 특정), FastAPI 서빙 구조가 구축되어 있음 — **2차 프로젝트가 이 도메인을 확장하는 것이라면 이 코드베이스를 ADK로 재구성하는 방향도 가능**. (단, 2차 프로젝트가 완전히 다른 주제라면 무관)

## 7. 다음 세션에서 할 일 (제안 순서)

1. 위 1번 섹션의 확인 필요 사항을 사용자에게 질문 (특히 공식 안내 문서 유무, ADK 필수 여부, 마감일)
2. 안내 문서가 있다면 파일 첨부 요청 → 요구사항 파악
3. 없다면: Part 2에서 배운 ADK/MCP/A2A 역량을 활용할 수 있는 프로젝트 주제 브레인스토밍 (PBL 1차와 동일하게, 사용자가 주제를 직접 제시하는 경우가 많았으므로 후보를 제시하되 확정은 사용자에게)
4. 주제 확정 후 4관문 검증(있다면): 데이터 실재성 / RAG·에이전트 구현 가능성 / 자연어 질문 성립 / 기간 내 완결 가능성
5. ADK 기반 스캐폴딩 시작 (root_agent 구조 설계 → 도구 정의 → 멀티 에이전트 필요 여부 판단 → 배포 대상 결정)

---

**새 세션 시작 멘트 예시**: "이 인수인계서(2차 프로젝트/인수인계.md) 내용대로야. 1번 섹션 확인 사항부터 나한테 물어봐."
