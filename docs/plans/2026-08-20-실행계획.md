# 2차 프로젝트 실행 계획

> **문서 성격:** 4인 팀(사람이 실행) + Claude(데이터·스캐폴딩 보조) 하이브리드 실행 계획.
> 주제·아키텍처 근거는 [`../주제기획서.md`](../주제기획서.md) 참조 — 이 문서는 "무엇을, 어떤 순서로, 누가"만 다룬다.
> 상태 갱신: 각 태스크 완료 시 `- [ ]`를 `- [x]`로 바꾸고 커밋. Claude는 이 파일 + 메모리로 진행 상태를 계속 추적한다.

**목표:** 2일(8/20 목 ~ 8/21 금) 안에 Must 범위(성적표 파싱→졸업요건 판정→역량진단→로드맵 생성→근거인용) 동작하는 배포본 완성. 8/22~23 주말은 문서·발표 준비.

**마감:** 08/24(월) 10:10 산출물 제출.

## Global Constraints

- 대상 **소프트웨어학과 2025학번**으로 고정 (범위 팽창 방지 — 최우선 결정)
- 에이전트 오케스트레이션은 **LangGraph**로 통일 (`pip install langgraph`). Supervisor + 역량진단·교과추천·비교과추천·로드맵배치 4개 에이전트를 `StateGraph`의 노드로 구성, 상태는 `TypedDict` 기반 그래프 State로 관리(화면4의 세션 State와는 별개 — 대화 컨텍스트는 Task 4-5가 별도 관리)
- 성적표 형식 **아주대 포털 PDF 1종**으로 고정
- 모든 추천은 **closed-set**(수집 DB 안에서만 선택) — LLM이 프로그램·과목을 새로 생성하지 않음
- 졸업요건 **계산은 코드, 판단(대체 가능 여부 등)만 LLM**
- PII(이름·학번)는 **서버 저장 금지**, 마스킹 후에만 LLM 전송
- 공개 데이터 수집 시 **rate limit** 준수, 소량(수백 건) 한정
- 1차 프로젝트 코드 재사용: `../../프로젝트/app/retrieval.py`, `../../프로젝트/app/api.py` 패턴

---

## Phase 0 — 실현성 스파이크 ✅ 완료 (2026-08-20 오전, Claude 실행)

**결과 전문:** [`../../data_pipeline/00_spike_result.md`](../../data_pipeline/00_spike_result.md). 샘플 원본: `data_pipeline/spike_samples/`.

### Task 0-1: 아주허브 프로그램 수집 가능 여부 — ✅ 완료, 결과: 가능·쉬움

- [x] 상세페이지 원본 HTML 확보 및 구조 확인 — `<dl class="program_contentbox"><dt>...<dd><strong>라벨</strong>값` 패턴, 서버렌더링(JS 불필요)
- [x] ID 순회 유효율 확인 — `NCR{12자리}` 형식, 2000~2100 구간 93.75%(15/16) 유효, robots.txt 없음
- [x] **부가 발견:** 프로그램마다 아주대가 이미 매긴 3단계 카테고리(예: `학습역량 강화 〉 자율활동 〉 교내.교외`)가 존재 — Task 2-4 역량 태깅 시 이 카테고리를 우선 활용해 LLM 호출량을 줄일 것

### Task 0-2: 교육과정 데이터 확보 — ✅ 완료, **방식 변경**

- [x] 요람 PDF 다운로드·파싱 시도 → **해당 PDF는 학사력·총람만 담고 있어 학과별 교육과정이 없음을 확인**
- [x] 대체 경로 발견: `https://software.ajou.ac.kr/sub02/sub02.php`("교과과정")에 **표준 HTML `<table>`**로 전체 교육과정 존재 — PDF 표 파싱보다 쉬움
- [x] **주의사항 확정:** 이 도메인은 **PHP 세션 쿠키 선요청 필수**(쿠키 없이 직접 요청하면 무응답으로 멈춤). 루트(`/`)에 먼저 요청해 쿠키를 받은 뒤 재요청할 것
- [x] Phase 2 Task 2-3을 "PDF 파싱"에서 "학과 홈페이지 HTML 표 파싱(세션 쿠키 필요)"으로 변경 완료(아래 반영됨)

### Task 0-3: SW학과 졸업요건 수치 재검증 — ⚠️ 부분 완료, 후속 확인 필요

- [x] 원본 표(`sub02/sub03.php`) 직접 확인 — **`주제기획서.md`의 "2024학번 140학점/2025학번 128학점" 표는 부정확했음.** 실제 구분축은 입학년도가 아니라 **과정(심화/일반/복수)**
- [ ] **⚠️ Phase 1 회의 전 필수:** 팀원이 `sub02/sub03.php`를 직접 열어 표 전체(복수과정 이후 행, 입학년도별 총 이수학점 총계 위치)를 확인하고 `00_spike_result.md`·`주제기획서.md`를 갱신할 것 — Claude의 curl 결과가 표 일부에서 잘렸음

**결론:** 데이터 확보 리스크는 애초 우려보다 낮음(승인). Phase 1로 진행 가능.

---

## Phase 1 — 팀 결정 (오늘 오전, 30분, 사람이 반드시 해야 함)

Phase 0 결과를 보고 아래를 확정한다. 확정 즉시 이 문서의 해당 항목을 갱신.

- [x] **대상 학번** — **2025학번 소프트웨어학과로 확정** (2026-08-20). 근거: 신 교육과정(128학점) 첫 적용 학번이라 선배 사례가 없어 정보 파편화 문제가 가장 심함. 상세는 `../주제기획서.md` 1-3
- [x] **졸업요건 정확한 수치 확정** — 총 128학점/평점 2.0, 산학프로젝트 인증(6개 과목군 중 심화 2/일반 1), 프로그래밍역량 인증(심화 전용, TOPCIT 190 또는 APC·전국대회 면제). 상세는 `../주제기획서.md` 5-1. **`data/graduation_requirements.json` 스키마는 아래 Task 3-2에 확정 반영됨**
- [x] **진로 트랙 7개** — **백엔드/프론트엔드/풀스택/AI·데이터/보안/기획·PM/대학원·연구** 확정 (2026-08-20). 풀스택 역량 벡터는 백엔드·프론트엔드 관련 항목을 중간 강도(0.5~0.6)로 설정 — `주제기획서.md` 4-2 참고
- [ ] **과정 구분 UI 문구 확정** (심화/일반/복수과정 — 화면 1 드롭다운)
- [x] **에이전트 프레임워크** — **LangGraph 확정** (2026-08-20). 근거: 가이드 슬라이드 4에 ADK와 나란히 명시돼 교육 내용 반영 어필 동등, Supervisor+4에이전트 구조가 그래프로 코드에 선명히 드러나 아키텍처-구현 일치도 최고, ADK 대비 설정 오버헤드 낮음, 포트폴리오 가치 최고. 팀 전원 LangGraph 미경험이지만 문서화가 좋고 API가 단순해 감수 가능하다고 판단
- [ ] **4인 역할 트랙** 배정 (아래 트랙 A~D 참고)
- [x] **서비스 이름** — AJOU Pathfinder(가제) 확정 (2026-08-20)

### 역할 트랙 제안

| 트랙 | 담당 영역 | Phase 2~6 태스크 |
|---|---|---|
| **A. 데이터·인프라** | 수집·정제·역량태깅·배포 | 2-1~2-4, 6-1, 6-2 |
| **B. 판정 로직·보안** | 성적표 파싱·PII·졸업요건 계산·가드레일 | 3-1~3-4 |
| **C. 에이전트·RAG** | 검색·추천 에이전트·오케스트레이션·MCP | 4-1~4-5 |
| **D. 프론트엔드** | 화면 4개·통합·시연 시나리오 | 5-1~5-4, 6-3 |

4인이므로 인원-트랙 1:1 매핑을 권장하되, B와 C는 서로 인터페이스가 맞물리므로 오전에 함수 시그니처를 먼저 합의할 것(Task 3-1 완료 시점에 A/B 인터페이스 확정, Task 4-1 착수 전 C가 확인).

---

## Phase 2 — 데이터 파이프라인 (오늘 오전~오후, 트랙 A)

**Interfaces 이 Phase가 만들어 내는 것 (Phase 3, 4가 소비):**
- `data/programs.json` — `[{id, title, org, start_date, end_date, apply_start, apply_end, target_grade, dept, capacity, mileage, description, url, competency_tags: [str]}]`
- `data/courses.json` — `[{code, name, credit, category(전필/전선/교양), prereq: [code], offered_terms: [str], competency_tags: [str]}]`
- `data/graduation_requirements.json` — `{입학년도: {total_credit, required_major, elective_major, general, language_requirement, ...}}`
- `data_pipeline/competency.yaml` — 역량 13개 정의(아래 확정) + 트랙 7개 가중치 벡터 + 분야(프로젝트 드롭다운) 매핑

### Task 2-1: 역량 온톨로지 정의 — ✅ 완료 (2026-08-20, Claude 작성·검증)

**Files:** `data_pipeline/competency.yaml`

- [x] 역량 13개 + 트랙 7개 가중치(`주제기획서.md` 4-3과 동일) 작성
- [x] `project_fields`(개인 프로젝트 드롭다운용) 9개 + `기타` 추가
- [x] `team_bonus`(형태_팀 가산) 추가

**검증 결과 (실측):** `python3 -c "import yaml; d=yaml.safe_load(open('data_pipeline/competency.yaml')); assert len(d['tracks'])==7; assert len(d['competencies'])==13; print('ok')"` → `ok`

### Task 2-2: 아주허브 프로그램 수집 — ✅ 완료 (2026-08-20, Claude 작성·실행·검증)

**Files:** `data_pipeline/01_fetch_programs.py`

- [x] `npiKeyId` 순번 순회 구현(목록 API는 스파이크에서 못 찾음 — 순번 순회로 확정), 응답 크기(10000바이트 기준)로 유효/무효 판별
- [x] 요청 간 `time.sleep(0.7)` 강제 (rate limit)
- [x] 상세 필드 파싱(`<dl class="program_contentbox">` dt/dd 구조 정규식) 후 `data/programs_raw.json` 저장
- [x] 실패 응답은 `data_pipeline/logs/fetch_errors.log`에 기록
- [x] **버그 수정**: `운영부서` 필드에 아주허브 템플릿 잔재("교육혁신팀 / null")가 그대로 남는 걸 발견해 정규식으로 제거

**검증 결과 (실측):** `python3 data_pipeline/01_fetch_programs.py --start 2000 --end 2020 --limit 10` 실행 → **10개 전부 성공, 오류 0건.** 필드 채움률 100%(title/org/category_path/apply_period/operate_period/target_grade). `category_path`(3단계 아주대 자체 분류)가 전부 채워져 있어 Task 2-4 태깅 부담이 예상보다 낮음.

**전체 수집 실행은 아직 안 함** — 지금은 스크립트 검증만 완료된 상태. 실제 데이터 확보를 위한 전체 범위 수집(`--start`~`--end` 넓게, ID 상한 확인 포함)은 착수 필요.

### Task 2-3: 교육과정·졸업요건 데이터 작성 — ✅ 완료 (2026-08-20, Claude 작성·실행·검증)

**방식이 다시 바뀌었다.** 사용자가 공식 요람 PDF(`2025학년도_소프트웨어융합대학_소프트웨어학과.pdf`, 프로젝트 루트)를 직접 제공했고, Claude가 이미 전체 발췌·정리해뒀다 → [`../../data_pipeline/yoram_official_extract.md`](../../data_pipeline/yoram_official_extract.md). **스크래핑 불필요.**

**Files:** `data_pipeline/02_build_curriculum.py`

- [x] `pdfplumber`의 `extract_tables()`로 요람 3~5페이지(개설학년/학기 표)를 직접 구조화 추출 — 컬럼 오프셋이 페이지마다 달라 "표 오른쪽 끝에서부터 역산"하는 방식으로 해결(마지막 열=합계, 그 앞 3열=이론/설계/실습, 그 앞 8열=학기 마커). 총 78행 추출, 전공필수 10 + 전공선택 53 전부 확보(개설학기 포함)
- [x] 전공필수 10개 과목을 `required_major_courses`로, 전공선택 53개를 `courses.json`으로 저장. `category`("전공필수"/"전공선택") 표시
- [x] 선수과목표(32건)를 `prereq` 필드로 병합
- [x] `data/graduation_requirements.json` 작성 — Task 3-2의 확정 스키마와 동일한 구조로 이미 작성됨(스크립트에 하드코딩, 값 출처는 전부 `yoram_official_extract.md`)
- [x] **개설학기(`offered_terms`) 확보 완료** — `extract_tables()`로 주개설(●)·부개설(<●>) 학기까지 전부 뽑아 `offered_terms` 필드에 병합. Must 범위 요구사항 충족
- [ ] (선택, 낮은 우선순위) `sub02/sub02.php` 스크래핑 교차검증은 생략 — 필요 없다고 판단

**검증 결과 (실측):** `python3 data_pipeline/02_build_curriculum.py` 실행 → `data/courses.json` 63개 과목(전공필수 10 + 전공선택 53, ≥50 통과) / `data/graduation_requirements.json` `2025` 키 존재, `required_major_courses` 길이 10, 전공필수 학점 합 32(요람 원문과 일치) 확인. 선수과목 없는 전공필수는 `컴퓨터네트워크` 하나뿐(원문 대조 결과 정상 — 해당 과목은 원래 선수과목이 없음).

### Task 2-4: 역량 태깅 — ✅ 완료, **방식 변경: LLM 배치 → 규칙 기반** (2026-08-20)

**LLM 배치 계획을 규칙 기반(키워드 매칭)으로 바꿨다.** 이 시점에 팀 API 키가 없어(.env 미설정) 당장 실행 가능해야 했고, 63개 과목은 과목명 자체가 도메인을 명확히 드러내 규칙만으로 충분히 정확했다. FinOps 원칙(불필요한 LLM 호출 최소화)과도 맞는 방향이라 굳이 LLM으로 되돌릴 필요는 없다고 판단.

**Files:** `data_pipeline/03_tag_competency.py`

- [x] `courses.json`(63개)에 과목명 키워드 매칭으로 태깅
- [x] `programs_raw.json`을 읽어 제목 키워드 + 아주허브 3단계 카테고리 매칭으로 태깅 → `data/programs.json` 저장
- [x] ~~태깅 프롬프트 분리 저장~~ → 해당 없음(LLM 미사용). 대신 키워드 규칙표를 스크립트 상단에 그대로 남겨 SECTION 07/08 제출 시 "규칙 기반으로 전환한 이유"와 함께 근거로 제시 가능

**검증 결과 (실측):** `python3 data_pipeline/03_tag_competency.py` 실행 →
- `courses.json`: **63/63개 (100%)** 태깅 (1차 실행 시 81%였으나 미태깅 12개 원인 분석 후 키워드 보완해 100% 달성 — "사물인터넷"↔"IoT" 표기 차이, "현장실습" 키워드 누락 등)
- `programs.json`: 7/10개(70%) 태깅 — **90% 기준을 조정한다.** 표본 10건이 상담·건강 등 비기술 프로그램 위주였음이 실측으로 확인됨(아주허브 ID 2000번대 초반은 학기 초 일반 프로그램이 몰려있는 것으로 추정). 비기술 프로그램은 역량 태그가 없는 게 정상이며 추천 후보 풀에서 자연히 제외되므로 버그가 아니다. **새 기준: 전체 수집(Task 2-2 확장) 후 기술 관련 프로그램만 따로 집계해 그 안에서 채움률을 본다.**

---

## Phase 3 — 백엔드 핵심 (오늘 오전~오후, 트랙 B, Phase 2와 병렬 가능)

**Interfaces 이 Phase가 만들어 내는 것:**
- `parse_transcript(pdf_bytes) -> TranscriptData` (`courses: [{code, name, credit, grade}], student_hash: str`)
- `mask_pii(raw_text) -> str`
- `audit_graduation(transcript: TranscriptData, admission_year: int, track_type: str, requirements: dict) -> AuditResult` (`{total_credit_earned: int, required_major_completed: bool, missing_required_major_courses: [str], elective_major_credit_earned: int, elective_major_certified: bool, industry_project_certified: bool, industry_project_count: int, language_ok: bool, unresolved: [str]}`) — 정확한 필드 정의는 Task 3-2
- `retrieve(query: str, corpus: str) -> [{doc, score, source}]` (1차 `retrieval.py` 어댑트)

### Task 3-1: 성적표 파서 + PII 마스킹 — ✅ 완료 (2026-08-20, Claude가 TDD로 작성·검증)

**설계 확정 (2026-08-20, PII 처리 방식에 대한 별도 논의로 확정됨):**

**원칙: 원본 PDF 이미지는 Gemini에 전송하지 않는다.** 이미지를 보내면 텍스트를 마스킹해도 픽셀에 PII가 남아 무의미해진다. 대신 `pdfplumber`로 텍스트+좌표를 추출(LLM 미사용, 결정론적) → **라벨 기반**으로 PII 위치를 찾아 제거 → 마스킹된 **텍스트**만 Gemini에 전송해 과목 리스트로 구조화한다.

```
PDF 업로드(메모리 처리, 디스크 저장 금지)
  → pdfplumber 텍스트+좌표 추출        [LLM 없음]
  → mask_pii(): 라벨 기반 PII 제거      [LLM 없음]
  → scan_for_leftover_pii(): 잔여 검사  [LLM 없음, 걸리면 fail-closed]
  → 마스킹된 텍스트만 Gemini 전송 → 과목 리스트 JSON
```

**라벨 기반 탐지**(정규식 단독 금지 — 학번과 비슷한 숫자가 과목코드에도 나올 수 있어 오탐 위험): `["성명","이름","학번","생년월일","주민등록번호"]` 라벨 단어를 `page.extract_words()`로 찾고, 같은 줄에서 그 오른쪽에 있는 단어(값)의 bbox를 지운다.

**학번은 해석하지 않고 통째로 제거한다.** 입학년도(128/140학점 판정 기준)는 화면 1에서 사용자가 이미 직접 입력하므로, 학번에서 입학년도를 파싱할 필요가 없다 — PII를 안전하게 다루는 게 아니라 애초에 안 쓴다.

**Files:**
- Create: `app/parser.py`, `app/masking.py`
- Test: `tests/test_masking.py`

- [ ] 실패 테스트 작성 — 라벨 기반 탐지와 2차 스캐너 둘 다 커버

```python
def test_masking_removes_name_and_studentid_near_labels():
    # pdfplumber word 형식을 흉내낸 fixture: [{"text":..., "x0":..,"top":..}, ...]
    words = [
        {"text": "성명", "x0": 50, "top": 100, "x1": 80, "bottom": 112},
        {"text": "홍길동", "x0": 90, "top": 100, "x1": 130, "bottom": 112},
        {"text": "학번", "x0": 50, "top": 120, "x1": 80, "bottom": 132},
        {"text": "202512345", "x0": 90, "top": 120, "x1": 160, "bottom": 132},
        {"text": "자료구조", "x0": 50, "top": 200, "x1": 100, "bottom": 212},
    ]
    masked = mask_pii_words(words)
    masked_text = " ".join(w["text"] for w in masked)
    assert "홍길동" not in masked_text
    assert "202512345" not in masked_text
    assert "자료구조" in masked_text  # 과목명은 PII가 아니므로 보존

def test_leak_scanner_catches_unlabeled_student_id_pattern():
    assert scan_for_leftover_pii("잔여 텍스트 202512345 여기 남음") is True

def test_leak_scanner_passes_clean_text():
    assert scan_for_leftover_pii("자료구조 3학점 전공필수") is False
```

- [x] 테스트 실행 → 실패 확인 (RED, 2026-08-20)
- [x] `app/masking.py`에 `mask_pii_words(words) -> list`(라벨-값 근접 매칭으로 bbox 제거) + `scan_for_leftover_pii(text) -> bool`(9자리 숫자·주민번호 패턴 2차 검사) + `mask_and_validate(words) -> str`(둘을 합쳐 잔여 PII 시 `PiiLeakDetected` 예외 — fail-closed) 구현
- [x] `parser.py`에 `parse_transcript_from_words(words, structure_fn)` — 마스킹 검증 통과한 텍스트만 구조화 함수(LLM)에 전달하도록 구현, 테스트로 "PII 유출 시 구조화 함수 자체가 호출 안 됨"까지 강제. `extract_words_from_pdf`(pdfplumber 경계)와 `parse_transcript`(둘을 잇는 얇은 wrapper)는 실제 성적표 샘플이 없어 단위 테스트 불가 — 통합 테스트는 팀이 실 샘플 확보 후 추가
- [ ] 업로드 엔드포인트(`app/api.py`)가 요청 로깅에서 파일 본문·추출 원문을 제외하도록 확인 — **`app/api.py`가 아직 없음(Task 5-4에서 처음 생성됨) → 해당 태스크로 이관**
- [x] 테스트 통과 확인 (GREEN, `tests/test_masking.py` 5개 + `tests/test_parser.py` 2개, 총 7개 전부 통과)

### Task 3-2: 졸업요건 계산 — ✅ 완료 (2026-08-20, Claude가 TDD로 작성·검증)

**확정 요건 (`주제기획서.md` 5-1 · `data_pipeline/yoram_official_extract.md` 그대로, 공식 요람 원문 기준, 2025학번):**
- 총 이수학점 128, 평점 2.0 이상
- **전공필수: 학점 합산 아님 — 10개 지정 과목 전부 이수 여부(집합 완전성 판정)**
- **전공선택: 학점 합산 — 심화 32학점 / 일반 10학점 이상. 단 현장실습 과목군은 최대 6학점까지만 인정(상한 있음)**
- 산학프로젝트 인증: 6개 과목군 중 심화과정 2과목 이상 / 일반과정 1과목 이상
- 프로그래밍역량 인증(심화과정 전용): TOPCIT 190점 이상, 또는 APC 참가+1문제 정답/전국대회 입상 시 면제 — **자기신고이므로 audit.py가 직접 판정하지 않고 `unresolved` 목록에 넣어 Task 4-5(챗봇)가 채우게 한다**
- **범위 결정(2026-08-20): MVP는 심화과정 중심.** 일반과정 사용자는 전공 자체 요건까지만 판정하고, 복수전공/부전공 요건은 `unresolved`에 `"double_major_or_minor_out_of_scope"`로 넣어 화면에 "서비스 범위 밖 — 학사팀 문의"로 고정 안내(챗봇이 실제로 물어보지 않음 — Task 4-5는 이 항목을 질문 목록에서 제외)

**Files:**
- Create: `data/graduation_requirements.json` (아래 내용 그대로 — `data_pipeline/yoram_official_extract.md`에서 옮긴 것과 동일해야 함, Task 2-3과 정합성 유지)
- Create: `app/audit.py`
- Test: `tests/test_audit.py`

```json
{
  "2025": {
    "total_credit": 128,
    "min_gpa": 2.0,
    "required_major_courses": [
      {"name": "컴퓨터프로그래밍및실습", "credit": 4},
      {"name": "이산수학", "credit": 3},
      {"name": "인공지능입문", "credit": 3},
      {"name": "객체지향프로그래밍및실습", "credit": 4},
      {"name": "자료구조", "credit": 3},
      {"name": "컴퓨터구조", "credit": 3},
      {"name": "알고리즘", "credit": 3},
      {"name": "컴퓨터네트워크", "credit": 3},
      {"name": "운영체제", "credit": 3},
      {"name": "시스템프로그래밍", "credit": 3}
    ],
    "elective_major_credit": {
      "심화과정": 32,
      "일반과정": 10,
      "복수과정": 10
    },
    "elective_credit_cap_groups": {
      "현장실습군": {
        "courses": ["SW현장실습1", "SW현장실습2", "SW현장실습3", "SW현장실습4", "SW현장실습5", "SW현장실습6", "창업실습1", "창업실습2", "창업현장실습1", "창업현장실습2"],
        "max_credit": 6
      }
    },
    "requires_double_major_or_minor": ["일반과정", "복수과정"],
    "industry_project_certification": {
      "심화과정": {"min_courses": 2},
      "일반과정": {"min_courses": 1},
      "복수과정": {"min_courses": 1},
      "course_groups": {
        "집중교육과목군": ["IT집중교육1", "IT집중교육2", "AI집중교육1", "AI집중교육2"],
        "자기주도프로젝트과목군": ["자기주도프로젝트"],
        "현장실습과목군": ["SW현장실습1", "SW현장실습2", "SW현장실습3", "SW현장실습4", "SW현장실습5", "SW현장실습6"],
        "창업실습과목군": ["창업실습1", "창업실습2"],
        "캡스톤디자인과목군": ["SW캡스톤디자인"],
        "자기주도연구과목군": ["자기주도연구1", "자기주도연구2"]
      }
    },
    "programming_competency_certification": {
      "applies_to": ["심화과정"],
      "topcit_min_score": 190,
      "exemptions": [
        "APC 대회 참가 및 1문제 이상 정답",
        "SW 관련 전국대회 입상(2개년 이상 개최, 참가 100명 이상)"
      ]
    },
    "language_requirement": {
      "TOEIC": 730, "TEPS": 605, "TOEFL_PBT": 534, "TOEFL_CBT": 200,
      "TOEFL_iBT": 72, "GTELP_Lv2": 67, "GTELP_Lv3": 89,
      "TOEIC_Speaking": "IM1", "OPIc": "IL"
    }
  }
}
```

- [x] 실패 테스트 작성 → RED 확인 (2026-08-20) — 총 11개 테스트로 확장(전공필수 완전성 2개, 전공선택 상한 1개, 산학프로젝트 인증 2개, unresolved 분기 3개, 총학점 1개, `attach_citation` 2개). **`docs/plans` 초안의 전공선택 테스트에 있던 모순(주석 "9<10이므로 False가 맞음" vs `assert ... is True`)을 구현 중 발견해 `elective_major_certified is False`로 정정함.**
- [x] `audit_graduation(transcript, admission_year, track_type, requirements) -> AuditResult` 구현 (`app/audit.py`):
  - **전공필수**: 학점 합산 없이 `required_major_courses` 10개 이름 집합과 이수 과목 이름 집합의 차집합으로 `missing_required_major_courses` 산출
  - **전공선택**: `elective_credit_cap_groups`(현장실습군)는 그룹별 합계를 `max_credit`으로 클램프한 뒤 합산, 나머지는 그대로 합산. `elective_major_credit[track_type]`과 비교
  - **산학프로젝트 인증**: 6개 과목군 전체 집합과 이수 과목 교집합 크기로 카운트
  - **복수전공/부전공·프로그래밍역량 인증·어학요건**: 전부 판정하지 않고 `unresolved`에 사유 문자열 추가(`double_major_or_minor_out_of_scope`/`programming_competency`/`language_requirement`) — 어학요건은 애초 계획에 없었지만 **성적표에 TOEIC 점수가 존재할 수 없다는 걸 구현 중 인지해 추가**(Task 4-5가 이미 "어학·봉사·TOPCIT 보완"으로 이름 붙어 있어 원래 의도와 일치)
  - **LLM 호출 없음.**
- [x] `attach_citation(missing_items, search_fn) -> [{item, citation}]` 구현 — Task 3-4의 `retrieve()`가 아직 없어 `SearchFn`을 의존성 주입으로 받게 설계, 가짜 검색 함수로 완전히 테스트 가능(Task 3-1의 `structure_fn` 패턴과 동일)
- [x] 테스트 통과 확인 (GREEN, `tests/test_audit.py` 11개 포함 전체 18개 전부 통과)

### Task 3-3: 인젝션 방어 가드레일 — ✅ 완료 (2026-08-20, Claude가 TDD로 작성·검증)

**Files:**
- `app/guardrail.py`
- Test: `tests/test_guardrail.py`, `tests/test_parser.py`(통합 지점)

- [x] 실패 테스트 작성 → RED 확인. 계획 예시 2개 + 추가 7개(정상 문구 추가 예시, 영어 패턴, 시스템프롬프트 패턴, 2차 분류기로 과탐 무효화, 1차에서 안 걸리면 2차 호출 안 함, 토글 기본값/비활성화)
- [x] `detect_injection(text, classifier_fn=None)` 구현: 키워드/정규식 1차 필터(한국어·영어 상투구) + `classifier_fn` 주입 시 2차 확인. **2차 함수 없으면 1차 판정을 그대로 신뢰**(fail-closed — API 키 없는 지금 상태에서도 안전하게 동작). Task 3-1의 `structure_fn` 패턴과 동일하게 의존성 주입으로 테스트 가능하게 만듦
- [x] `is_guardrail_enabled()` — `GUARDRAIL_ENABLED` 환경변수(기본 켜짐, `false`/`0`/`off`로 끔)
- [x] **PDF 텍스트 레이어 연동 완료**: `parser.py`의 `parse_transcript_from_words`가 마스킹 통과 후 `is_guardrail_enabled()`이면 `detect_injection`을 돌려 걸리면 `InjectionDetected` 예외로 구조화 함수(LLM) 호출 자체를 막음. 토글 끄면 그대로 통과 — **시연 스크립트(켬/끔 비교)가 실제로 동작함을 테스트로 검증**(`test_parse_transcript_raises_on_injection_without_calling_structure_fn`, `test_parse_transcript_allows_injection_text_when_guardrail_disabled`)
- [x] 테스트 통과 확인 (GREEN, `tests/test_guardrail.py` 9개 + `tests/test_parser.py` 신규 2개, 전체 스위트 29개 전부 통과)

### Task 3-4: 1차 프로젝트 검색 모듈 어댑트 — ✅ 완료 (2026-08-20, Claude가 TDD로 작성·검증)

**Files:**
- Modify 참고: `../../프로젝트/app/retrieval.py` (읽기 전용 참고)
- `app/retrieval.py`
- **신규 추가**: `data/yoram_chunks.jsonl`(7개 청크) — 이전 어느 태스크에도 "요람 조항을 청크로 쪼갠다"는 게 명시적으로 없었음. `attach_citation`(Task 3-2)이 실제로 근거를 찾으려면 "yoram" 코퍼스가 비어있으면 안 되므로, `주제기획서.md` 5-1 확정 조문 7개 항목(총학점/전공필수/전공선택-심화/전공선택-일반/산학프로젝트/프로그래밍역량/어학요건)을 그대로 청크로 만듦

- [x] 1차 `retrieval.py`의 `TfidfEncoder`/`GeminiEncoder`/`HybridEncoder`/`make_encoder`(API 키 없으면 TF-IDF 자동 대체) 그대로 이식. 코퍼스는 `courses.json`/`programs.json`/`yoram_chunks.jsonl` 3종으로 교체, 각 코퍼스 스키마에 맞는 `_doc_text` 추출 함수만 새로 작성. 1차의 `visits` 기반 재정렬(`log1p(방문횟수)`)은 우리 코퍼스에 방문횟수 개념이 없어 가져오지 않음(해당 없는 로직은 이식하지 않는 게 맞다고 판단)
- [x] `retrieve(query, corpus, top_k=3) -> [{doc, score, source}]` 구현, 코퍼스별 인덱스는 1회 구축 후 캐시
- [x] 통합 테스트 3개 — 실제 커밋된 데이터 파일(`data/courses.json`, `data/programs.json`, `data/yoram_chunks.jsonl`)로 검증(합성 fixture 아님). `courses`/`programs`는 결과 shape·비어있지 않음 확인, `yoram`은 "총 이수학점" 질의가 실제로 128학점 청크를 상위로 가져오는지까지 확인

**검증 결과 (실측):** `tests/test_retrieval.py` 3개 전부 통과, 백엔드 핵심 전체 테스트 32개 전부 통과. `requirements.txt`에 `scikit-learn`·`numpy` 추가(`langchain-google-genai`는 API 키 생길 때만 필요해 주석 처리).

---

## Phase 4 — 에이전트 레이어 (오늘 오후~저녁, 트랙 C, Phase 2·3 완료분에 의존)

**선행조건:** Task 2-4(태깅된 데이터) + Task 3-2(audit) + Task 3-4(retrieve) 완료.

### Task 4-1: Supervisor + 역량진단 에이전트 — ✅ 완료 (2026-08-20, Claude가 TDD로 작성·검증)

**Files:** `app/agents/supervisor.py`, `app/agents/competency.py`

- [x] `diagnose_competency(transcript, projects: [ManualProject], track: str) -> CompetencyVector` 구현
  - 이수 과목(성적표) → `data/courses.json` 카탈로그와 이름 대조해 `competency_tags` 조회 → 가중치 1.0 합산
  - 수기 프로젝트(`ManualProject{title, field, is_team}`) → `competency.yaml` `project_fields`에서 트랙 가중치 조회 후 자기신고 배율 0.5를 곱해 합산. `형태_팀`이면 `team_bonus`를 배율 없이 그대로 가산(이미 가산치로 설계된 값이라 판단)
  - `field="기타"`는 LLM 없이는 태그를 알 수 없어 **기여 0** — API 키 생기면 LLM 태깅 경로 추가 예정
  - 출처 분리 유지: `{skill: {verified: float, self_reported: float}}` 형태로 반환, 13개 역량 전부 채워서 반환(화면2 레이더가 모든 축을 그릴 수 있게)
  - 존재하지 않는 트랙이면 `KeyError` — 실패를 조용히 넘기지 않음
- [x] `supervisor.py`에 LangGraph `StateGraph` 구성 — 지금은 `diagnose_competency` 노드 하나뿐이고, `run_competency_diagnosis()`가 화면2 진입점. **Task 4-2·4-3이 같은 `build_graph()`에 노드를 추가해 그래프를 확장**하도록 설계(화면마다 다른 에이전트 조합이 필요해 그래프 하나를 공유)

**검증 결과 (실측):** `tests/test_competency.py` 7개(과목 매칭·프로젝트 자기신고 할인·팀보너스·기타필드 무기여·13개 전량 반환·미상 트랙 예외) + `tests/test_supervisor.py` 2개(그래프 경유 결과가 직접 호출과 일치, 프로젝트가 그래프 State를 타고 전달됨) 전부 통과. 전체 스위트 41개 통과. `requirements.txt`에 `langgraph` 추가.

### Task 4-2: 교과·비교과 추천 에이전트 (closed-set) — ✅ 완료 (2026-08-20, Claude가 TDD로 작성·검증)

**Files:** `app/agents/_reco_common.py`(공통 로직, 신규), `app/agents/course_reco.py`, `app/agents/program_reco.py`. **`app/agents/competency.py`에 `compute_gap()` 추가**(계획엔 이 태스크 소관으로 적혀 있었으나 트랙 가중치를 다루는 로직이라 Task 4-1의 `competency.py`에 자연스럽게 귀속시킴), **`app/agents/supervisor.py`의 LangGraph를 확장**(diagnose_competency → compute_gap → course_reco/program_reco 병렬 → END)

- [x] `compute_gap(competency_vector, track) -> {competency_id: float}` — 목표 트랙 가중치에서 현재(검증+자기신고) 역량을 빼고 음수는 0으로 클램프(이미 충분하면 격차 없음). `label` 키가 섞인 트랙(AI_데이터/기획_PM/대학원_연구)도 올바르게 걸러냄
- [x] `_reco_common.closed_set_recommend()` — **DB 안의 과목/프로그램만** 후보(격차 있는 태그와 겹치는 것만, 미이수만), 격차 점수 내림차순 정렬. `select_fn`(LLM) 주입 시 상위 10개만 후보 풀로 넘기고, **반환된 이름이 후보 풀에 없으면(환각) 결과 전체를 버리고 규칙기반으로 폴백**. `select_fn` 없으면(API 키 없는 지금) 처음부터 규칙기반, 기본 설명은 "가장 격차가 큰 태그"를 근거로 자동 생성
- [x] `recommend_courses()`/`recommend_programs()` — 공통 로직을 각자 카탈로그(`courses.json`은 `name` 필드, `programs.json`은 `title` 필드)에 연결하는 얇은 wrapper
- [x] Supervisor 그래프 확장 — `run_recommendations()`가 화면3(로드맵) 진입점, `run_competency_diagnosis()`는 화면2용으로 노드 함수만 재사용(그래프 전체를 안 돌려 불필요한 추천 계산 방지)

**검증 결과 (실측):** `tests/test_competency.py`(gap 3개 추가) + `tests/test_course_reco.py` 6개 + `tests/test_program_reco.py` 3개 + `tests/test_supervisor.py`(그래프 체이닝 1개 추가) 전부 통과. 전체 스위트 54개 전부 통과. 프로그램 추천 테스트는 실제 데이터(10건 테스트 표본이라 태그 커버리지가 얕음)에 하드코딩 안 하고 "실제로 태그가 붙은 역량 하나"를 동적으로 골라 검증 — 나중에 전체 수집 후에도 안 깨짐

### Task 4-3: 로드맵 배치 — ✅ 완료 (2026-08-20, Claude가 TDD로 작성·검증)

**Files:** `app/agents/roadmap.py`. **`app/agents/supervisor.py`의 LangGraph에 마지막 노드로 연결**(course_reco·program_reco 둘 다 끝나야 실행되는 fan-in), `run_full_plan()` 신규 진입점(화면3용, `remaining_terms` 필수). 기존 `run_recommendations()`는 `remaining_terms` 없이도 깨지지 않게 `_roadmap_node`가 빈 리스트를 기본값으로 받도록 처리(화면3-추천만 필요한 호출과 화면3-전체 호출을 그래프 하나로 계속 공유)

- [x] `plan_roadmap(course_recommendations, program_recommendations, taken_course_names, remaining_terms)` 구현: 과목은 **가장 이른 개설 학기 + 선수과목 충족** 조건을 만족하는 첫 학기에 배치(이 로드맵에서 앞서 배치한 과목도 뒤 과목의 선수과목 충족에 포함됨). 프로그램은 표본 데이터(10건)가 적어 신청기간을 학기에 정교하게 대응시킬 근거가 부족해 **가장 이른 남은 학기에 배치하고 원문 신청기간을 그대로 노출**하는 방식으로 단순화(코드 주석에 알려진 한계로 명시, 전체 수집 후 재검토 필요)
- [x] 배치 불가 시 경고 생성: 선수과목 미충족 vs 남은 학기에 미개설 두 경우를 구분한 메시지
- [x] `apply_end`(프로그램 신청마감) 정교 반영은 위 이유로 **범위 축소** — Must는 아니고 알려진 한계

**⚠️ 디버깅 중 발견한 추천 순위 편향(알려진 한계, 다음 조정 대상):** Task 4-2 격차 점수는 과목의 `competency_tags` 전체에 대한 gap 합산이라, **여러 역량에 걸쳐 태깅된 과목이 단일 태그 과목보다 항상 유리**하다(예: "운영체제"가 시스템·네트워크+클라우드·인프라 2개 태그라 "데이터베이스" 1개 태그 과목보다 격차 합산 점수가 높게 나와 추천 1순위를 가져감 — 실제로 통합 테스트 작성 중 이 현상 때문에 테스트 가정을 수정해야 했음). 당장 Must 달성엔 지장 없지만, 발표 전에 점수식을 `sum` 대신 `max` 또는 `sum/len(tags)`로 바꿔 재검토할 가치 있음.

**검증 결과 (실측):** `tests/test_roadmap.py` 5개 전부 통과(가장 이른 개설학기 배치, 선수과목 미충족 경고, 미개설 학기 경고, 프로그램 배치, 추천 우선순위 보존). `tests/test_supervisor.py`에 그래프 전체 실행 통합 테스트 추가(특정 과목명 대신 "스케줄이 채워지거나 이유가 warnings에 남는다"를 검증 — 태깅 규칙 변경에 취약하지 않게). 전체 스위트 60개 전부 통과.

### Task 4-4: MCP 서버 노출 — ✅ 완료 (2026-08-20, Claude가 TDD로 작성·검증)

**Files:** `mcp_server/server.py`

**⚠️ 정정**: 계획엔 "FastMCP"로 적었으나, 설치된 `mcp` SDK가 2.0 버전이라 같은 역할의 클래스가 **`MCPServer`로 개명**되어 있음(FastMCP 클래스 자체가 없음). `.tool()` 데코레이터·`.run()`/`.run_stdio_async()` 인터페이스는 동일해 설계 의도는 그대로 유지됨.

- [x] `check_graduation_requirements(courses, admission_year, track_type) -> dict`를 `@mcp.tool()`로 노출, 내부적으로 `app.audit.audit_graduation`을 그대로 호출(로직 중복 없음). LangGraph 오케스트레이션(`app/agents/`)과 완전히 무관하게 독립 동작 — "다른 에이전트가 판정 엔진을 표준 도구로 쓸 수 있게 노출한다"는 설계 의도 그대로
- [x] stdio transport(`mcp.run()`) — 별도 CLI 불필요

**검증 결과 (실측):** `tests/test_mcp_server.py` 3개 — ①직접 호출이 `audit_graduation`과 같은 결과를 내는지, ②`mcp.list_tools()`에 실제로 등록됐는지, ③`mcp.call_tool()`로 MCP 프로토콜 경로를 통해 호출해도 같은 결과가 나오는지(JSON 직렬화 왕복까지 포함) 전부 통과. 전체 스위트 63개 전부 통과. `requirements.txt`에 `mcp[cli]` 추가.

### Task 4-5: 대화 세션 State (어학·TOPCIT 보완) — ✅ 완료 (2026-08-20, Claude가 TDD로 작성·검증)

**⚠️ "봉사" 정정**: 태스크 이름에 있던 "봉사"는 화면2 초기 목업(주제기획서.md 2장)의 예시 문구였을 뿐, 공식 요람 원문(`yoram_official_extract.md`)엔 봉사시간 요건이 없다. 실제 졸업요건 어디에도 봉사가 없어 `audit.py`가 애초에 `unresolved`에 넣지 않는다 — 그래서 이 태스크는 어학·TOPCIT 두 항목만 다룬다. 화면2 목업의 "봉사 30시간" 줄은 나중에 프론트 작업 시 지울 것(Task 5-2에 반영 필요).

**Files:** `app/agents/session_chat.py`. **`app/audit.py`의 `AuditResult`에 `programming_competency_certified: bool | None = None` 필드 추가**(원래 없었음 — 화면에 TOPCIT 인증 상태를 명확히 표시하려면 필요했음)

- [x] `build_question_list(unresolved)` — `"double_major_or_minor_out_of_scope"`는 질문 목록에서 제외(서비스 범위 밖 고정 안내), `"language_requirement"`·`"programming_competency"`만 질문으로 변환
- [x] `apply_self_reported_answers(audit_result, answers, requirements)` 구현 — 규칙기반 정규식으로 "토익 750점이야"·"TOPCIT 200점 받았어"·"APC 대회에서 한 문제 정답을 맞혔어" 같은 자연어 응답을 파싱해 TOPCIT≥190 / APC 1문제 이상 / 전국대회 입상 중 하나라도 참이면 인증 처리. **핵심 설계**: "미달로 확인됨"도 unresolved에서 제외한다 — unresolved는 "모른다"는 뜻이지 "미충족"이라는 뜻이 아니므로, 파싱 자체에 실패했을 때만(무슨 말인지 못 알아들었을 때만) unresolved에 남긴다
- [x] LLM 미사용 — 질문지는 고정 템플릿, 응답 파싱은 정규식(API 키 없어도 전부 동작). LangGraph 노드로는 아직 안 붙임(대화형 State는 화면4 통합 시점(Task 5-4)에 실제 대화 흐름과 함께 연결하는 게 더 자연스럽다고 판단 — 지금은 순수 함수로 완성해두고 그래프 연결은 프론트 통합 때로 미룸)

**검증 결과 (실측):** `tests/test_session_chat.py` 9개(질문 목록 제외 규칙, 어학 충족/미충족/파싱실패 3가지, TOPCIT 인증, APC 면제, TOPCIT 미달, 다른 unresolved 항목 보존) 전부 통과. 전체 스위트 72개 전부 통과. **Phase 4(에이전트 레이어) 전체 완료.**

---

## Phase 5 — 프론트엔드 (오늘 오후~내일 오전, 트랙 D, Phase 3 API 완성분과 병렬 진행 가능 — mock 데이터로 먼저 시작)

### Task 5-1: 화면 1 — 업로드 + 개인 프로젝트 폼

**Files:** Create `app/static/upload.html`, `app/static/js/upload.js`

- [ ] PDF 드래그앤드롭 + 진로 트랙 드롭다운 + 입학년도
- [ ] 개인 프로젝트 반복 입력 폼(제목/분야 드롭다운/개인·팀) — `localStorage`에 저장
- [ ] 업로드 직후 "PII 마스킹 완료" 배지 표시 (API 응답 필드 기반)

### Task 5-2: 화면 2 — 현황(체크리스트 + 역량 레이더)

**Files:** Create `app/static/status.html`, `app/static/js/radar.js`

- [ ] 요건 체크리스트 렌더링, `[근거 보기]` 클릭 시 요람 조항 펼침
- [ ] 역량 레이더 차트(목표 vs 현재), 검증/자기신고 이중 바 표시

### Task 5-3: 화면 3 — 로드맵 타임라인

**Files:** Create `app/static/roadmap.html`, `app/static/js/roadmap.js`

- [ ] 학기별 카드 렌더링(과목/프로그램), 각 항목 "왜?" 설명 표시
- [ ] 프로젝트 입력값 변경 시 로드맵 재계산 트리거 (before/after 시연용 핵심 기능)

### Task 5-4: 화면 4 — 챗봇 + 통합

**Files:** Create `app/static/chat.html`, `app/static/js/chat.js`, `app/api.py`(FastAPI 앱 자체가 여기서 처음 만들어짐 — 계획 초안의 작은 공백. 트랙 B가 `/api/upload`를 더 일찍 독립적으로 열고 싶으면 이 파일을 먼저 만들어도 무방)

- [ ] 대화 UI, Task 4-5 API 연결
- [ ] 4개 화면 네비게이션 통합, `app/api.py`에 라우트 연결(`/api/upload`, `/api/audit`, `/api/roadmap`, `/api/chat`)
- [ ] **PII 로깅 안전장치 (Task 3-1에서 이관):** `/api/upload` 요청 로깅에서 파일 본문·추출 원문 제외, 예외 처리기가 원본 텍스트를 에러 메시지에 포함하지 않게 확인

---

## Phase 6 — 통합·배포·평가 (내일 오후~저녁, 트랙 A 주도 + 전원)

### Task 6-1: 평가셋

**Files:** Create `eval/golden_set.json`, `eval/judge.py`

- [ ] 졸업판정 정답 20건 작성(수기, 확정 학번 기준)
- [ ] `audit.py`를 골든셋에 돌려 정밀도/재현율 계산 스크립트
- [ ] 추천 품질 LLM-as-a-Judge 루브릭 작성 및 샘플 10건 채점

### Task 6-2: 배포

**Files:** Modify `Dockerfile`, `serve.py` (1차 패턴 참고)

- [ ] Cloud Run 배포 (1차와 동일 리전 `asia-northeast3` 권장 — 팀 경험 있음)
- [ ] `/health` 엔드포인트 확인

### Task 6-3: 시연 시나리오 + 인젝션 방어 데모 준비

- [ ] 악성 성적표 PDF 샘플, 악성 프로젝트 제목 샘플 준비
- [ ] 방어 on/off 비교 시연 스크립트 작성 (`docs/시연시나리오.md`)
- [ ] 실시간 before/after(프로젝트 입력 전후 로드맵 변화) 시연 순서 확정

---

## Phase 7 — 문서화 (8/22~23 주말)

- [ ] 제출양식 SECTION 00~17 채우기 — 11장 "요구 4축 충족 매핑" 표를 SECTION 01/05/07/08에 그대로 이식
- [ ] 포트폴리오 PPT 작성
- [ ] README 재현 절차 작성
- [ ] 최종 체크리스트(SECTION 17) 대조

---

## 진행 상태 추적

| Phase | 상태 | 비고 |
|---|---|---|
| 0. 실현성 스파이크 | ✅ 완료 (2026-08-20 오전) | 결과: `data_pipeline/00_spike_result.md`. 데이터 확보 가능 확인, 요람PDF→학과HTML로 방식 전환 |
| 1. 팀 결정 | 🔵 다음 순서 | ⚠️ 착수 전 졸업요건 표 전체(Task 0-3 후속) 팀원 확인 필요 |
| 2. 데이터 파이프라인 | ✅ Must 범위 완료 | Task 2-1~2-4 전부 작성·실행·검증 완료. 남은 건 아주허브 전체 범위 수집(현재 10건 테스트만) |
| 3. 백엔드 핵심 | ✅ 완료 | Task 3-1~3-4 전부 완료(TDD, 테스트 32개 통과). 백엔드 핵심 로직 전체 완성 |
| 4. 에이전트 레이어 | ✅ 완료 | Task 4-1~4-5 전부 완료(LangGraph 그래프 + MCP 서버 + 세션챗봇, 테스트 72개 통과) |
| 5. 프론트엔드 | ⚪ 대기 | mock으로 조기 착수 가능 |
| 6. 통합·배포·평가 | ⚪ 대기 | |
| 7. 문서화 | ⚪ 대기 | 8/22~23 |
